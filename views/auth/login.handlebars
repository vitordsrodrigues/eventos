<div class="container d-flex justify-content-center align-items-center" style="min-height: 85vh; padding-top: 2rem;">
  <div class="col-xl-10">
    <div class="card rounded-4 border-0 shadow-lg">
      <div class="row g-0">
        
        <div class="col-lg-6 d-flex align-items-center gradient-custom order-lg-1 order-1">
          <div class="text-white px-4 py-5 p-md-5 mx-md-4 text-center">
            <i class="fas fa-user fa-4x mb-4"></i>
            <h3 class="mb-4 fw-bold">Bem-vindo de volta!</h3>
            <p class="mb-0 opacity-75">Por favor, entre na sua conta para continuar acessando nossos serviços.</p>
          </div>
        </div>

        <div class="col-lg-6 order-lg-2 order-2">
          <div class="card-body p-md-5 mx-md-4">
            <div class="text-center mb-5">
              <h4 class="fw-bold">Login</h4>
              <p class="text-muted">Entre na sua conta</p>
            </div>

            <form action="/login" method="POST">
              <div class="form-floating mb-4">
                <input 
                    type="email" 
                    class="form-control rounded-3 {{#if invalidField}}{{#if (eq invalidField 'email')}}is-invalid{{/if}}{{/if}}" 
                    id="email" 
                    name="email" 
                    placeholder="Digite seu email"
                    value="{{email}}"
                    required 
                />
                <label for="email">Email</label>
                {{#if invalidField}}
                    {{#if (eq invalidField 'email')}}
                    <div class="invalid-feedback">
                        <i class="fas fa-exclamation-circle"></i> {{error}}
                    </div>
                    {{/if}}
                {{/if}}
              </div>

              <div class="form-floating mb-4">
                <input 
                    type="password" 
                    class="form-control rounded-3 {{#if invalidField}}{{#if (eq invalidField 'password')}}is-invalid{{/if}}{{/if}}" 
                    id="password" 
                    name="password" 
                    placeholder="Digite sua senha" 
                    required 
                />
                <label for="password">Senha</label>
                {{#if invalidField}}
                    {{#if (eq invalidField 'password')}}
                    <div class="invalid-feedback">
                        <i class="fas fa-exclamation-circle"></i> {{error}}
                    </div>
                    {{/if}}
                {{/if}}
              </div>

              <div class="d-grid gap-2 mb-4">
                <button type="submit" class="btn btn-primary btn-lg rounded-3 text-white">
                  Entrar
                </button>
              </div>

              <div class="text-center">
                <p class="mb-0 text-muted">
                  Não tem uma conta? 
                  <a href="/register" class="text-primary text-decoration-none fw-bold">Cadastrar</a>
                </p>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
